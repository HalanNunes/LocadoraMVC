@model ProjetoLocadoraMVC3.Models.ATOR

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ATOR</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.CD_ATOR)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CD_ATOR)
            @Html.ValidationMessageFor(model => model.CD_ATOR)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.NM_ATOR)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NM_ATOR)
            @Html.ValidationMessageFor(model => model.NM_ATOR)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
