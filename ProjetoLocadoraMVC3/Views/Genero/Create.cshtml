@model ProjetoLocadoraMVC3.Models.GENERO

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>GENERO</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.CD_GENERO)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CD_GENERO)
            @Html.ValidationMessageFor(model => model.CD_GENERO)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DS_GENERO)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DS_GENERO)
            @Html.ValidationMessageFor(model => model.DS_GENERO)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
