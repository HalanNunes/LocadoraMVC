@model ProjetoLocadoraMVC3.Models.GENERO

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>GENERO</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.CD_GENERO)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CD_GENERO)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.DS_GENERO)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DS_GENERO)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
